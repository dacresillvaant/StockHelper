spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_ai
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

#OTHER
playwright:
  headless: true

scheduler:
  heatmap:
    cron: "0 */30 * * * *"

  process-currencies: "0 0 8,16 * * *"
  low-price-alert: "0 15 21 ? * MON-SAT"
  owned-stock-alert: "0 15 8 ? * SAT"